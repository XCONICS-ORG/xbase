# Shared Turtle env.
# Copy to env/.env for local development if needed.

FLAG_SHOW_BETA_FEATURE=false
APP_ENV=development
APP_WEB_PORT=3002

# Optional production URL used by @turtle/seo.
PRODUCTION_URL=

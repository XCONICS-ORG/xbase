# Xbase Storybook

Component previews for `@xbase/design-system`.

```bash
bun --filter @xbase/storybook dev
```

Storybook runs on `http://localhost:6006` by default. Set `STORYBOOK_PORT` to use another port.
